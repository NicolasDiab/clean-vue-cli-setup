export default class Book {
  constructor (name, isbn, numberOfPages, country, released) {
    this.name = name;
    this.isbn = isbn;
    this.numberOfPages = numberOfPages;
    this.country = country;
    this.released = released;
  }
}
